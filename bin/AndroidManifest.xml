<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csl.demo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="14" />
	<uses-permission android:name="android.permission.READ_CONTACTS" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActOne"></activity>
        <activity android:name=".ActTwo"></activity>
        <activity android:name=".ActThree"></activity>
        <activity android:name=".ActFour"></activity>
        <activity android:name=".ActFive"></activity>
        <provider android:name=".DatabaseContentProvider"
            	  android:authorities="com.csl.demo.databases.provider"/>
    </application>

</manifest>
